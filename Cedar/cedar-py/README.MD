# CedarPy
Cedar has its CLI to validate ant test the policy against a request, so python is not necessary But, if you want to use the (Rust) Cedar Policy library from Python you can use cedarpy library 

## Install the package

`pip install cedarpy`